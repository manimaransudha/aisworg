# CR-112 — Schema implementation Gaps

**Raised:** 2026-09-25 · **Origin:** core/packs.ts has zero references to schema_definitions/schema validation — same hand-coded-check pattern as Service Definition.
schema_definitions rows (compiled via schemaCompiler.ts) are consumed only by formGenerator.ts/sdkAuthoring.ts — purely to render the authoring form (field widgets, groups, Ontology-composable fields, referential sources). Never to validate submitted content.
No Ajv or any JSON-Schema validator exists in the codebase at all.
So the gap is platform-wide, not Service-Definition-specific: schema_definitions is a form-generation grammar only. Every authored-entity validate*Seed/validate* function (Pack, Template, Profile, Deliverable Definition, Service Definition) hand-codes its own field checks in TS, duplicating what the schema already declares (required, type, min/max, enum-via-x-ontology) — the stored schema is never the source of truth actually enforced at write time.

**Status:** ✅ Closed 2026-09-25.
 

# Direction 

This is what should  happen
1. The table schema has to match the schema definition. Otherwise this can diverge very quickly. 
2. Form generator has to use the schema which is already the case. (hoping). The buttons on the form follow the transition definitions
3. Everytime there is an insert/update, the validation has to be against the schema. 
Do not code. Let us start with Packs/Template/Profile(all the three use sdk)

## Deepdive on packs

Findings for Pack/Template/Profile (all "use SDK" per your framing), checked against your 3 requirements:

1. Table schema must match schema_definitions — currently diverged, in three places, not two:

packs.category CHECK constraint (migration 002): hardcoded enum ('Platform','Organisation','Domain','Compliance','Technology','Integration').
schema_definitions Pack v1 (migration 014): same enum, hardcoded again in the JSON.
validatePackSeed (actual code path run on insert): validates category against Ontology category:pack concepts instead — CR-020 moved this off the hardcoded list.
Three independent sources of truth for the same field; only the third is live. The table CHECK and the schema_definitions enum are both now stale relative to Ontology, and could silently reject or admit different values than the code path allows.
templates/profiles tables carry no such enum drift themselves (no category-like field), but both still only have status CHECK constraints — no schema_definitions row ever cross-checked against them either.
2. Form generator uses the schema — confirmed true. formGenerator.ts/sdkAuthoring.ts (generateFields, groupFieldsForDisplay, ontologyComposableFieldsIn, etc.) all read schema_definitions to render the authoring form. Buttons: possibleNextStates comes from transition_definitions via transitionEngine, not hardcoded — matches your requirement.

3. Validation on insert/update against the schema — does not happen.

A real generic validator exists: validateAgainstSchema(schema, content) in formGenerator.ts.
It's called only in the web/draft layer (sdkAuthoring.ts): on /save (non-blocking — advisory only, draft still saves) and on /import (blocking).
core/packs.ts (validatePackSeed), and by the same pattern core/templates.ts/core/profiles.ts, never call validateAgainstSchema or touch schema_definitions at all — the actual DB insert/update path (packsDB.create, presumably templatesDB.create/profilesDB.create) is validated purely by hand-written TS checks, independent of the schema.
So the schema is enforced only as a soft advisory during draft editing in the web UI, and not at all if a row is created any other way (direct core-layer call, seed script, API).
Want me to check core/templates.ts and core/profiles.ts in the same depth to confirm they mirror Pack's pattern exactly, or is this enough to proceed to design options?


## Design 

Full: single source of truth, DB constraint removed entirely, all validation funneled through validateAgainstSchema
No enum/pattern logic in SQL at all. core/packs.ts / templates.ts / profiles.ts call validateAgainstSchema(latestSchemaFor(kind), seed) as a mandatory step inside validate*Seed, replacing today's hand-written field checks (Ontology-backed checks like assertCanonicalCategory stay, since schema_definitions can't express "value must exist in Ontology concept type X" — needs a new x-ontology-driven branch in validateAgainstSchema itself, which ontologyComposableFieldsIn already partially supports).
New concept: schema_definitions becomes binding, not advisory, at every insert/update path (core layer, not just web draft save).
Files touched: core/packs.ts, core/templates.ts, core/profiles.ts, formGenerator.ts (extend validateAgainstSchema to also drive Ontology checks), migrations to drop the now-redundant table CHECKs.
Risk: largest change; touches the actual insert/update code path for all three entities, must not regress existing Ontology-specific validation.

1. Drop all table constraints. 
2. dblayer/*DB.ts create, insert, update should validate against the schema whether the data is imported from a json or comes from authoring. 
- x-ontology is checked for form dropdowns already. It is used to fetch valid options for rendering, not to check a submitted value is one of them. Same Ontology lookup, different call site: validateAgainstSchema needs to call it too, but that's reuse, not new plumbing. 
- No schema version pinning on validate — an update must validate against the row's own schema_definition_id/version, not always latest, or a schema bump breaks existing valid rows retroactively.
- no new column needed. Per Ch.5 §13/§19.9 (CR-018), compatibility is already declared in packs.metadata (JSONB): 
supportedPlatformVersion,minSupportedPlatformVersion, maxSupportedPlatformVersion, (nothing for the current implementation) 
incompatiblePackVersions, 
migrationGuidance.(nothing for the current implementation)  
And dependencies[] already has an incompatible type for pack-to-pack (not version-to-version).

## Design steps

Applies to any authored-entity kind that uses the SDK (schema_definitions + dblayer *DB.ts). Pack is the first pass; same steps repeat per entity.

1. Drop hardcoded enum/CHECK constraints from the table where the field is Ontology-governed (e.g. `packs.category`) — table keeps structural constraints only (NOT NULL, type, FK, UNIQUE).
2. Update the corresponding `schema_definitions` row: replace hardcoded `enum` with `x-ontology` (the same marker formGenerator already reads for dropdown options), so the schema stops being a second hardcoded copy.
3. Extend `validateAgainstSchema` to check `x-ontology`-marked fields against real Ontology data (reuse the same lookup formGenerator uses for options, not new plumbing).
4. Add a `schema_definition_id` column to the entity's own table (e.g. `packs`) — does not exist today, only `deliverable_authoring_content` tracks it (pre-publish draft stage). Set it on create; this is the prerequisite for the next step.
4a. Add schema-version pinning to validation: validate an update against the row's own authored `schema_definition_id`/version (from step 4), not always the latest schema version.
5. Split today's hand-written `validate*Seed` checks into two buckets:
   - Shape/Ontology-membership checks (semver pattern, category/classification/code-via-dynamic-category-suffix must be a real Ontology value — `code`'s check already expressed in schema via `x-referential-source-by`/`x-referential-source-suffix`, migration 133) — these are now covered by step 3. Delete them from `validate*Seed`.
   - DB-lookup checks that schema can't express (code+version+tenant uniqueness, dependency target must resolve to a real Active Pack) — these move into a helper module in core/ (e.g. `core/validators/<entity>.ts`).
6. `dblayer/*DB.ts` create/update calls the helper before doing the DB operation — helper imports core (e.g. `core/ontology.ts`), dblayer imports the helper. No cycle: dblayer never imports back into `core/packs.ts` etc.
7. Once (1)-(6) land for an entity, its `validate*Seed`-equivalent is schema-driven and enforced at every write path (core call, seed script, import, API), not just the web draft-save advisory.

Deferred, not part of this pass: enforcing `packs.metadata` compatibility fields (`supportedPlatformVersion`/etc.) at composition — declared but unenforced, separate gap (Ch.5 §19.9), not a schema-sync issue.

## Built (Pack, first pass)

- migration 266: dropped `packs_installation_classification_check` (category's own CHECK was already dropped, migration 038); added `packs.schema_definition_id` (backfilled to current Pack schema).
- `core/ontology.ts`: new `validateOntologyFieldsAgainstSchema(schema, content, viewer)` — walks `x-ontology: true` fields (top-level + one level into referential-list item rows), resolves concept type (static `x-referential-source`/`x-referential` or dynamic `x-referential-source-by`+suffix), checks the real value via the existing `assertCanonicalCategory`.
- `core/packWriteValidator.ts` (new file, flat under `core/`, not a `validators/` subfolder — no existing precedent for that nesting): `validatePackWriteAgainstSchema` — fetches the pinned (`schemaDefinitionId`) or latest Pack schema, runs `validateAgainstSchema` (shape) + `validateOntologyFieldsAgainstSchema` (Ontology membership) + a code+version+tenant uniqueness check (raw query, not `packsDB`, to avoid a `packsDB` <-> helper import cycle).
- `packsDB.ts`: `create`/`updateDraftContent` call the validator before the INSERT/UPDATE and return `{ error }` without writing on failure; `create` stamps `schema_definition_id` to the current latest Pack schema; `updateDraftContent` pins to the row's own existing `schema_definition_id`.

Scope actually built vs. the steps above: steps 1-4a and 7 done in full. Step 5/6 done only for the class of checks steps 1-3 actually cover (category, code-via-category, installationClassification, compositionStrategy, contributionCapabilities[].code, contributionCompetencies[].dimension/value) plus uniqueness. NOT done: removing the now-redundant duplicate `assertCanonicalCategory` calls for those same fields out of `core/packs.ts`'s own `validatePackSeed` (harmless duplication, left in place rather than risk touching that ~460-line function's surrounding logic), and NOT moving the rest of `validatePackSeed` (quality gates/review gates/checklists/policies/obligations/duplicate-detection/composition-arity — all genuine DB-lookup checks, but a much larger surface than what this pass scoped) into the helper. Those checks still only run via the explicit `core/packs.ts` call path, unchanged.

## Deepdive on templates

Same 3 requirements, checked against `templates` table (migrations 002/059/062), `schema_definitions` (entity_kind='Template', migrations 015/045/054/058/061/076/078/079/080/083/160/162/164/172/208), `core/templates.ts::validateTemplateSeed`, and `templatesDB.ts`.

1. Table schema vs `schema_definitions` — **no divergence found**. `templates` carries only structural constraints: `status` lifecycle CHECK (Draft/Validated/Published/Active/Deprecated/Retired/Archived — matches Pack's own kept-structural status CHECK) and `UNIQUE(code, template_version, tenant_id)`. Unlike `packs.category`, there is no Ontology-governed field duplicated as a table-level enum. Step 1 is already satisfied — nothing to drop.

2. `schema_definitions` vs hardcoded enums — **already correct, done ahead of this exercise**. Template's two Ontology-governed fields are already marked `x-ontology: true`, not a hardcoded `enum`:
   - `code` → concept type `template-categories` (migration 054, CR-021).
   - `deliverableCatalogue[].code` → concept type `deliverable-name` (migration 160, CR-087 — this migration explicitly replaced a plain, unvalidated `x-referential` with `x-ontology: true` for the same reason CR-020/CR-086 did for Pack).
   The six `<category>PackCodes[].packCode` fields use `x-referential: "pack-code:<Category>"` — correctly NOT `x-ontology`, since a Pack code isn't an Ontology concept; it's an entity reference, checked by a real DB lookup (`validateTemplateSeed`, `packsDB.findByCode` + category match) — that's a step-5-bucket-2 check, not a schema-expressible one.

3. Write-time Ontology enforcement — `validateOntologyFieldsAgainstSchema` (`core/ontology.ts`) is already entity-kind-agnostic: it walks any `x-ontology: true` field, top-level and one level into array-item rows. Template's two `x-ontology` fields fit that exact shape already. **Step 3 needs zero new code for Template** — pure reuse, as designed.

4. `schema_definition_id` column — **not present** on `templates`. Not done.
4a. Schema-version pinning on validate — depends on 4. Not done.

5. Split `validateTemplateSeed`'s hand-coded checks:
   - Now-redundant once steps 4/6 land (covered by step 3's reuse): the two `assertCanonicalCategory` calls at `core/templates.ts:521` (`code`) and `:567` (`deliverableCatalogue[].code`).
   - Genuine DB-lookup/derived-state checks that stay (schema can't express these, same as Pack's quality-gate-class checks): pack-slot code+category resolution (:526-531), `exposedParameters` candidate-membership (:547-555), `dependencyGraph` referential checks (:579-594), cycle detection (:602-605), Template Inheritance superset + locked-edge checks (:615-663).

6. `templatesDB.ts` write-path wiring — **not done**. `createDraft`/`updateDraftContent`/`setDraftContent`/`upsert` call no validator; no schema check runs on any DB-layer write path.

7. Not done — enforcement today is still web-draft-save-advisory (`sdkAuthoring.ts`) plus `validateTemplateSeed`'s own hand-coded checks at publish, same pre-fix gap Pack had.

Net difference from Pack: Template's steps 1-3 gap was already closed (CR-021/CR-087 put the Ontology-governed fields on `x-ontology` before this exercise even started) — no table CHECK to drop, no schema enum to convert. The real gap is entirely the plumbing: steps 4/4a/6/7, plus deleting the two now-duplicate `assertCanonicalCategory` calls once that plumbing lands.

### Proposed build scope (mirrors Pack's "Built" pass; not yet built)

- Migration: add `templates.schema_definition_id`, backfilled to current Template schema. No CHECK to drop (none exists).
- New `core/templateWriteValidator.ts` (same reasoning as `core/packWriteValidator.ts` — outside `templates.ts` so `templatesDB.ts` can import it without importing `templates.ts`): `validateTemplateWriteAgainstSchema` — fetch pinned (`schemaDefinitionId`) or latest Template schema, run `validateAgainstSchema` (shape) + `validateOntologyFieldsAgainstSchema` (`code`, `deliverableCatalogue[].code`) + a `code` + `template_version` + `tenant_id` uniqueness check (raw query, avoiding a `templatesDB` <-> helper import cycle).
- `templatesDB.ts`: `createDraft`/`updateDraftContent` call the validator before INSERT/UPDATE, return `{ error }` without writing on failure; `createDraft` stamps `schema_definition_id` to latest; `updateDraftContent` pins to the row's own existing `schema_definition_id`.
- `core/templates.ts`: delete the two now-redundant `assertCanonicalCategory` calls (`code`, `deliverableCatalogue[].code`) from `validateTemplateSeed`. Everything else in `validateTemplateSeed` (pack-slot/exposedParameters/dependencyGraph/inheritance checks) stays untouched — same DB-lookup-class carve-out Pack's own pass left in place.

## Built (Template, first pass)

- migration 267: added `templates.schema_definition_id` (backfilled to current Template schema; no CHECK to drop, per the deepdive above).
- `core/templateWriteValidator.ts` (new file, mirrors `core/packWriteValidator.ts`): `validateTemplateWriteAgainstSchema` — fetches the pinned (`schemaDefinitionId`) or latest Template schema, merges `{code, name, templateVersion}` (real columns that are also schema properties) with `draftContent`, runs `validateAgainstSchema` (shape) + `validateOntologyFieldsAgainstSchema` (reused unchanged — Template's `code`/`deliverableCatalogue[].code` already fit its existing top-level/one-level-into-array-items walk) + a code+templateVersion+tenant uniqueness check (raw query, avoiding a `templatesDB` <-> helper import cycle).
- `templatesDB.ts`: `createDraft`/`updateDraftContent` call the validator before the INSERT/UPDATE and return `{ error }` without writing on failure; `createDraft` stamps `schema_definition_id` to the current latest Template schema; `updateDraftContent` pins to the row's own existing `schema_definition_id`. Both existing call sites (`core/templates.ts`, `core/sdkAuthoring.ts`) already destructure `{ data, error }`, so no caller changes were needed.
- `seuTypes.ts`: `TemplateRow.schema_definition_id: string | null` added.

**`setDraftContent` now wired too (owner: "it has to also be changed. I do not want you to go be create/update based on names")** — every real write to `templates` that carries schema-governed content gets validated, regardless of method name, not just the two named `create*`/`update*`. `setDraftContent(id, draftContent)` now fetches the row's own `code`/`name`/`template_version`/`tenant_id`/`schema_definition_id` first (it never changes those columns itself), validates, and returns `{ error }` without writing on failure — same as `createDraft`/`updateDraftContent`. Its only caller, `materialisePackSelectionsAndCapabilities` (called from `materialiseTemplateDraft`, called from `publishTemplate` and from `sdkAuthoring.ts`'s Draft->Validated hop), now returns `{ ok: false; errors }` instead of `void` and propagates up through all 4 call sites (`publishTemplate`'s two branches, `reactivateAsNewVersion`, `sdkAuthoring.ts`), each already shaped to carry an error back (`PublishTemplateResult` / `TransitionTemplateResult` / the authoring result type) — no caller needed a new error-handling branch invented, only a check added.

Checked and deliberately left alone: `templatesDB.upsert` (raw ON CONFLICT insert, no `draft_content` write) is still unvalidated — but unlike `setDraftContent`, it's not in the governed path: `cleanSlate.ts` (the `db:clean-slate` pipeline) already routes through `publishTemplate`/`profilesDB`'s own governed entry points, not `upsert`, by its own explicit prior design (see its own comment at line 636-639). Only `seedEbookLibraryPilot.ts`, a separate legacy/pilot seed script, still calls `templatesDB.upsert` directly — a pre-existing deviation from the governed path, not something this pass introduced, and out of today's scope. Flagging it rather than silently fixing or silently leaving it undocumented.

`setDeliverableCatalogue` (writes the real materialised `deliverable_catalogue` column) and `updateStatus` (lifecycle-only) stay unvalidated by design — same carve-out as Pack's own join-table writes (materialised/relational content, not the JSON `draft_content` blob `schema_definitions` actually describes).

Scope actually built vs. the steps above: steps 1-3 (already true beforehand), 4/4a/6 done in full — every `draft_content`-writing path (`createDraft`, `updateDraftContent`, `setDraftContent`) is now validated. Step 5/7: `validateTemplateSeed`'s two `assertCanonicalCategory` calls (`code`, `deliverableCatalogue[].code`) are now genuinely redundant (mirrors Pack's own harmless-duplication call) but were left in place, not deleted — same conservative choice Pack's own pass made, not re-litigated here.

## Deepdive on profiles

Same 3 requirements, checked against `profiles` table (migrations 002/064), `schema_definitions` (entity_kind='Profile', migrations 015/045/066/173/174/175/199/229/245/246/208), `core/profiles.ts::validateProfileSeed`, and `profilesDB.ts`. Profile's surface is bigger than Pack/Template's — 9 Configuration Parameters, several referential-list fields, and multiple write methods (`upsert`, `create`, `createDraft`, `setDraftContent`, `updateDraftContent`) — so this deepdive is more granular before proposing a build.

1. Table vs `schema_definitions` — **no CHECK-level divergence**. `profiles.status` carries the same structural lifecycle CHECK Pack/Template have. `profiles.category` and `profiles.environment` are plain TEXT, no CHECK. `profiles.category` is a **dead column** — CR-091 Part 3 (migration 175) explicitly stopped writing it from every write path ("do not delete. But do not use them"); the schema's own `category` field was removed the same migration. Not a divergence to fix — a deliberate deprecation, already fully executed on both sides.

2. `schema_definitions` vs hardcoded enums / missing markers — **mixed, one confirmed real gap, one already-fine surprise, one open design question:**
   - **Gap (confirmed):** `environment` still carries a hardcoded `"enum": ["development","staging","production"]` from migration 015 — never converted, even though migration 166 explicitly created the `category:environment` Ontology concept type for this exact purpose ("Profile's own `environment` field... already has a fixed enum... but is NOT Ontology-backed... promoted to a real Ontology concept type") and then never finished the schema-side conversion. This is Profile's equivalent of Pack's pre-fix `category`.
   - **Already correct (no work needed):** all 9 `CONFIGURATION_PARAMETER_FIELDS` (`targetCloudProvider`, `primaryProgrammingLanguage`, `sourceControlProvider`, `deploymentStrategy`, `aiProviderPreference`, `defaultRepositoryStructure`, `documentationLevel`, `developmentMethodology`, `domain`) are already `x-ontology: true` with a fixed `x-referential-source` (migrations 175/199), plus `additionalCapabilityCodes[].capabilityCode` and `participatingOrganisationCodes[].organisationCode` (migrations 173/175). These get write-time enforcement for free, same as Template's two fields.
   - **Open design question, not a bug:** `featureFlagCodes[].featureCode` is `x-referential: "feature-flag"` but deliberately NOT `x-ontology: true` — migration 173's own comment says so explicitly ("`x-ontology:true` (unlike featureFlagCodes, since capability-name concepts carry real labels worth resolving)"), i.e. at the time, `x-ontology` was treated as a display/label concern, not a write-enforcement one. `validateProfileSeed` still hand-checks it against the `feature-flag` concept type today (line 460-463) — it IS Ontology-governed in practice, just not marked that way in schema. Converting it would be a real, deliberate reversal of a documented decision, not a mechanical fix — flagging for a decision rather than doing it silently.

3. Write-time Ontology enforcement (`validateOntologyFieldsAgainstSchema` reuse) — same entity-agnostic walk as Pack/Template, zero new code needed for the fields already marked `x-ontology`. Once `environment` is converted (2), this becomes **the first real server-side enforcement `environment` has ever had** — `validateProfileSeed`'s own check today (line 419) is `if (!seed.environment?.trim())`, presence-only, no membership check at all. Not a duplication cleanup like Pack/Template's `code` fix — an actual gap being closed.

4. `schema_definition_id` column — not present on `profiles`. Not done.
4a. Version pinning — depends on 4.

5. `validateProfileSeed`'s hand-coded checks, sorted:
   - Becomes redundant once (2)+(6) land: `environment` presence-only check (weaker than what schema enforcement would give — worth upgrading, not just deleting), the 9 `CONFIGURATION_PARAMETER_FIELDS` membership half of the loop (lines 482-492 — the *value-membership* half only; the *is this parameter mandatory for this tenant* half is a live per-tenant Ontology flag lookup, not schema-expressible, stays), `additionalCapabilityCodes` (471-474), `participatingOrganisationCodes` (518-521).
   - Stays regardless (genuine DB-lookup / cross-field logic, not schema-expressible): `baseTemplateCode` resolves to a real Template (424-429); `exposedParameterOverrides` candidate-membership against the base Template's own overridable set (436-444); `optionalPackCodes`/`PACK_SELECTION_SLOTS` Pack-code+category resolution (446-458, same as Template's own pack slots); the mandatory-flag half of the Configuration Parameter loop (488-491); `primaryProgrammingLanguage`'s cross-vocabulary check against the *different* `technology` concept type (508-513 — a consistency check between two vocabularies, not a single membership check); `knowledgeLocations`' exactly-one-of-`deliverableCode`/`capabilityCode` XOR logic (527-533, though each field's own membership check, once XOR-resolved, could be marked `x-ontology` and reused — the XOR itself can't be).
   - `featureFlagCodes` (460-463) stays as-is unless (2)'s open question is resolved to convert it.

6. `profilesDB.ts` write-path wiring — **not done**, and Profile has more write paths than Template to get right (the Template pass's own lesson: check every real write, not just ones named `create*`/`update*`):
   - `createDraft` — the interactive-authoring Draft-creation path. Needs wiring.
   - `updateDraftContent` — interactive-authoring Save-while-Draft path. Needs wiring.
   - `setDraftContent` — the actual seed/publish materialisation path (`materialiseProfileDraft`'s equivalent, called by `publishProfile`), same role `templatesDB.setDraftContent` had. Needs wiring — this is the one most worth getting right, same reasoning as Template.
   - `upsert` — raw ON CONFLICT insert, direct test-fixture / legacy-seed path (`publishProfile` no longer calls it, per its own comment, mirroring Template's `upsert`). Same disposition as `templatesDB.upsert`: not on the governed path if `cleanSlate.ts` already routes through `publishProfile` — needs the same one-line confirmation Template's did before deciding to leave it alone.
   - `create` — a small, undocumented-until-now case: `findOrCreateDefaultProfile`'s throwaway fallback synthesizer (a minimal Active row: random code, `baseTemplateId`, `environment` only, nothing else). No `draft_content` at all, so the only schema-governed field it ever writes is `environment` — worth a decision on whether it's in scope or is a deliberate throwaway.

7. Not done — same pre-fix gap as Pack/Template: enforcement today is web-draft-save-advisory (if that route exists for Profile the same way) plus `validateProfileSeed`'s own hand-coded checks at publish, and for `environment` specifically, no real enforcement at all yet.

### Questions before proposing a build scope — resolved

1. `environment` → Ontology-driven AND composable (owner). `featureFlagCodes[].featureCode` → `x-ontology: true` (owner, reversing migration 173's original "labels vs. enforcement" distinction). `profilesDB.create` (the `findOrCreateDefaultProfile` synthesizer) → validated like every other real write (owner: "It has to follow the same validation" — used by `commissioning.ts` (2 call sites, real SEU commissioning fallback) and `web/objectives.ts`, not test-only). `profilesDB.upsert` confirmed out of scope the same way Template's was — `cleanSlate.ts` already routes through `publishProfile`, not `upsert` directly (only `seedEbookLibraryPilot.ts` still does, same disposition as Template's, left alone).

## Built (Profile, first pass)

- migration 268: converted `environment` from a hardcoded `enum` to `x-ontology: true` + `x-ontology-composable: true` sourced from `category:environment` (the concept type migration 166 created for this exact field but never wired into the schema) — mirrors Pack's own `code` composability (CR-079/CR-100): write-time validation never rejects an unregistered `environment` value; an author-entered one gets proposed instead via `proposeComposableOntologyValues`, wired into `sdkAuthoring.ts`'s Profile `createAuthoringDraft`/`updateAuthoringDraft` branches (Profile had no composable-proposal wiring at all before this — net-new, not a mechanical copy). Also converted `featureFlagCodes[].featureCode` to add `x-ontology: true` (not composable — hard reject-on-unregistered, matching `additionalCapabilityCodes`). Added `profiles.schema_definition_id`, backfilled to latest.
- `core/profileWriteValidator.ts` (new, mirrors `core/templateWriteValidator.ts`): `validateProfileWriteAgainstSchema` — pinned/latest Profile schema, `validateAgainstSchema` + `validateOntologyFieldsAgainstSchema` (reused unchanged — every Ontology-governed Profile field profiles already had turned out to already be `x-ontology`-marked except the two fixed in this pass) + code+profileVersion+tenant uniqueness.
- `profilesDB.ts`: **all four** real content-writing methods validated, not just the ones named `create*`/`update*` (this session's own standing lesson from Template) — `create` (the `findOrCreateDefaultProfile` throwaway), `createDraft`, `updateDraftContent`, and `setDraftContent` (the actual seed/publish materialisation path, same as Template's). `updateDraftContent`/`setDraftContent` fetch the row's own `code` first since neither receives it as input (Profile's `code` is a system UUID, never editable — migration 045).
- `core/profiles.ts`: `materialiseProfileDraft` returns `{ ok: false; errors }` instead of swallowing `setDraftContent`'s error; propagated through all 4 call sites (`publishProfile`'s two branches, `reactivateAsNewVersion`, `sdkAuthoring.ts`'s advance-draft path) — same shape-matching each caller already had, no new error-handling branch invented.
- `seuTypes.ts`: `ProfileRow.schema_definition_id: string | null` added.

Not touched, by design (mirrors Template's own carve-outs): `profilesDB.upsert` (confirmed out of governed path), `setPackSelection` (materialised join-table writes, not the JSON `draft_content` blob), `updateStatus` (lifecycle-only). `validateProfileSeed`'s hand-coded checks (environment presence, `featureFlagCodes`, `additionalCapabilityCodes`, `participatingOrganisationCodes`, the Configuration Parameter membership half) were left in place, not deleted — same conservative choice as Pack/Template.

### Fallout: Template's own `purpose` field, never actually wired

`db:clean-slate` broke after the Template build pass: `failed to publish template "saas-product": "purpose" is required`. Root cause — `purpose` has been in Template's schema `required` list since migration 061 (CR-023), but `validateTemplateSeed` never checked schema-`required`-ness at all (purely hand-coded checks), and `TemplateSeedInput`/`seedSdlcStandardTemplates.ts`/`seedCr104Demo.ts` never had a `purpose` field in the first place — every seed-script-published Template has been missing it since CR-023 shipped, silently, because nothing ever enforced `required` at write time until this pass's `templateWriteValidator.ts`. Not a regression in the new validator's logic — the first real enforcement of an already-declared requirement, the same class of gap this whole exercise exists to surface.

(Correction to an earlier claim in this session: `exposedParameters` is NOT in the same boat — it IS already wired through `seedSdlcStandardTemplates.ts` (`exposedParameters: templateSeed.exposedParameters`) and defaulted/persisted by `materialisePackSelectionsAndCapabilities`. The code comment citing "exposedParameters (and `purpose`) never persisted" describes a historical bug that was already fixed for `exposedParameters` specifically; only `purpose` was left behind.)

Fixed by backfill (owner's choice): added `purpose?: string` to `TemplateSeedInput` (core/templates.ts), `purpose: string` to both seed scripts' own Template seed types (`seedSdlcStandardTemplates.ts`, `seedCr104Demo.ts`) and wired through to their `publishTemplate` calls, and backfilled real `purpose` text into all 10 affected `*.template.json` files (the 9 SDLC-standard ones + `cr104-demo-minimal`) — reusing the canonical per-category "what it's for, when to choose it" guidance already authored in `ontology_concepts` (migration 057, `template-categories` descriptions) for the 9 whose `code` matches a real category 1:1; a custom sentence for `cr104-demo-minimal` (not a real category — a test fixture). `seedEbookLibraryPilot.ts`'s own `ebook-library.template.json` left untouched (out of scope, standalone/unwired, same disposition as its `upsert` calls).

**Second layer of the same bug, caught before it surfaced**: the type/JSON backfill alone wasn't enough — `publishTemplate`'s own `templatesDB.createDraft` call never passed `draftContent` at all, so `purpose` still wasn't in the content the validator saw at that write. Fixed by passing `draftContent: { purpose: seed.purpose }` there (`materialiseTemplateDraft` overwrites `draft_content` in full moments later regardless, so this only has to satisfy the first write's own validation). Checked `publishProfile` for the identical class of gap proactively: its `profilesDB.createDraft` call has the same shape and Profile schema requires `baseTemplateCode` — but `baseTemplateCode` is never a column-mapped param (only `base_template_id`, the resolved FK, is), so it would have failed the same way the moment `db:clean-slate` reached a Profile publish. Fixed the same way: `draftContent: { baseTemplateCode: seed.baseTemplateCode }`. `reactivateAsNewVersion`/`copyProfileAsNewDraft` (both Template and Profile) already pass `{ ...seed }` as `draftContent`, so they were never affected.

## Deepdive on Policy

Same 3 requirements, checked against `policy_definitions` table (migration 167 + 207/216 follow-ups), `schema_definitions` (entity_kind='Policy'), `core/policyDefinitions.ts::validatePolicyDefinitionSeed`, and `policyDefinitionsDB.ts`. Policy Definition (CR-089, Ch.24) is the 4th SDK-authored entity — same family as Pack/Template/Profile, standalone (no relationship to any other entity, owner's own words), 7-state lifecycle.

1. Table vs `schema_definitions` structural CHECKs — **no divergence to fix**. `policy_definitions` carries `constraint_type CHECK (IN 'Policy','Standard')` and `scope CHECK (IN 'Transition','Eligibility')` — both mirror the schema's own plain `enum` (not `x-ontology`) for the same fields: these are fixed structural vocabularies (like `status`), not Ontology business vocabulary, so per Design Steps step 1 ("drop... where the field is Ontology-governed") there is nothing to drop here. `category` IS Ontology-governed (`x-ontology: true`, `category:policy`) and correctly carries no table CHECK. Same "already correct" finding as Template's own step 1.

2. **A live data-integrity finding, a different class of gap than Pack/Template/Profile's own step-2 findings**: this DB's `schema_definitions` row for Policy is stuck mid-migration-chain, not merely un-Ontology'd. There is no `schema_migrations`-style tracking table in this codebase (confirmed — migrations are applied as raw files, per the standing "migration replay is fragile, apply directly" note), so the only way to know what actually landed is to read live state directly:
   - Only **version=1** exists (`SELECT version FROM schema_definitions WHERE entity_kind='Policy'` → one row). Migration 207's `INSERT INTO schema_definitions ... version=2 ... WHERE NOT EXISTS` never took — every later migration's `WHERE version = MAX(version)` guard has therefore always resolved to version 1 and mutated it in place instead of the version 2 the migration files assume they're building on.
   - At the TABLE level, 207 and 216 both fully applied (`policy_definitions.scope` column exists with its CHECK; `applicability_deliverable_names`/`governed_transition`/`governing_condition` columns are gone, per 216's `DROP COLUMN`s) — so the DDL half of the chain ran.
   - At the schema_definitions JSON level, migration 216's own big `UPDATE ... jsonb_set` DID apply (`conditions[]` carries the full per-condition redesign — `applicabilityDeliverables`, `severity`, `governingCondition`, `relatedObligations`, `exceptionRules`, all present, in 216's own `x-property-order`). But **everything from 217 onward appears not to have applied to the schema JSON**: confirmed via a direct query — `conditions[].requiredEvidence` still has 216's original `{evidenceType: enum[document,email,governed], evidenceFormat}` shape, not 224's real, Ontology-backed `{title, category (→ category:evidence, x-ontology:true), description, collectionMethod}` replacement. `code` has no `x-ontology`/`x-referential-source-by-value` marker (confirmed empty), consistent with 209/228 (whichever touched `code`) also not having applied. `scope` itself is entirely **absent from `schema_definitions.properties`** — even though it's a real, hand-validated column and drives `conditions[].applicabilityDeliverables[].name`'s dynamic source (`x-referential-source-by-value` keyed off `scope`) — because 207's version-2 INSERT (the only migration that ever defined `scope` as a schema property) never landed, and 212/213's own later patches are self-guarded on `scope` already existing (`AND schema->'properties' ? 'scope'`), so they silently no-op'd too.
   - Net effect: `validatePolicyDefinitionSeed`'s own hand-coded `evidence.category` check (Ch.17 §8, migration 224's intended shape) is validating a field the LIVE schema doesn't even declare yet. Building write-time schema enforcement (`validateAgainstSchema`) on top of today's live schema as-is would enforce the wrong, stale shape.

3. Ontology-governed fields actually present live, and whether `validateOntologyFieldsAgainstSchema`'s existing walk reaches them:
   - `category` (top-level) → `category:policy`. Reachable (matches Pack/Template's own depth).
   - `applicabilityEnvironments[]` (top-level array) → `category:environment`. Reachable (matches Profile's own multi-select fields).
   - `conditions[].severity` (one level into array items) → `category:policy-condition-severity`. Reachable (matches Template's own array-item depth).
   - `conditions[].relatedObligations[].category/origin/priority/severity` — **two levels of array nesting** (`conditions` → item → `relatedObligations` → item → field). `validateOntologyFieldsAgainstSchema` (`core/ontology.ts:91-118`) walks top-level + exactly one level into array-item rows — confirmed by reading it — so this depth is **not reached by the existing, entity-agnostic walker**. First real gap in that walker's own coverage; Pack/Template/Profile never had a field this deep.
   - `conditions[].applicabilityDeliverables[].name` — not `x-ontology` at all, by design: it's `x-referential-source-by-value`, a dynamic concept-type lookup keyed off the sibling `scope` field (Transition → `deliverable-name`/Ontology; Eligibility → a real Authority Vocabulary noun, NOT Ontology). `validateOntologyFieldsAgainstSchema` has no notion of a by-value/dynamic source today — every prior `x-ontology` field across Pack/Template/Profile is statically sourced. Extending the walker to resolve this would be new capability, not reuse.
   - `conditions[].exceptionRules[].exceptionApprovers[]` — `x-referential` (not `x-ontology`), correctly: real Authority Vocabulary badges, a different registry than Ontology concepts. Nothing to enforce here via the Ontology path.
   - `conditions[].requiredEvidence.category` — SHOULD be `x-ontology: true` → `category:evidence` per migration 224's intent, but isn't live yet (see finding 2).

4. `schema_definition_id` column — not present on `policy_definitions`. Not done.

5. `validatePolicyDefinitionSeed`'s hand-coded checks, sorted (once (2) is resolved and (4)/(6) land):
   - Coverable by schema reuse at the walker's current depth: `category`, `applicabilityEnvironments`, `conditions[].severity`.
   - Coverable only if the walker is extended for 2-level array nesting: `conditions[].relatedObligations[].category/origin/priority/severity`, `conditions[].requiredEvidence.category` (once schema is repaired).
   - Not schema-expressible at all today (would need dynamic by-value support, a genuinely new mechanism): `conditions[].applicabilityDeliverables[].name`.
   - Stays regardless, genuine DB-lookup/cross-field logic (mirrors Pack's own quality-gate-class carve-outs): code+version+tenant uniqueness; `scope` presence/enum (could become a plain schema `enum` check with zero Ontology involvement, once (2) restores it as a property); `validateGoverningCondition`'s per-type shape rules; `exceptionRules[].exceptionApprovers` membership against the live badge vocabulary (not Ontology); `applicabilityDeliverables[].transitions` membership (real `transition_definitions` rows, DB-lookup); `parentPolicyDefinitionId` resolution.

6. `policyDefinitionsDB.ts` write paths — smaller surface than Profile's: only **`createDraft`** and **`updateDraftContent`** write real content (no `setDraftContent`, no `upsert` — Policy Definition has no separate publish-time materialisation step; `updateStatus` is lifecycle-only). Both are already gated by `validatePolicyDefinitionSeed` one layer up, at the `sdkAuthoring.ts` call site (`createAuthoringDraft`/`saveAuthoringDraft`/`advanceAuthoringDraft`'s publish hop) — unlike Pack/Template/Profile's own pre-fix state, Policy's web-layer check is already **blocking**, not advisory. The gap is still real, though, same reasoning as the other three: `policyDefinitionsDB.ts` itself validates nothing, so any writer that bypasses `sdkAuthoring.ts` (a seed script, a test fixture, a future API route) writes ungoverned — the whole point of this exercise is enforcement at the DB layer itself, not reliance on every caller remembering to call `validate*Seed` first.

**Item (a) resolved, not a design gap**: re-checked the full migration file chain (207 through 268) — it's internally consistent. 207's `version=2` INSERT is guarded by a trivial `WHERE NOT EXISTS` that passes on any in-order first run; every later migration's `WHERE version = MAX(version)` then correctly resolves to version 2. A clean sequential `migrate:seu`/`db:clean-slate` converges to the right shape (`scope` present, `requiredEvidence.category` → `x-ontology`, etc.). The drift is specific to this one dev DB never having had migration 207 applied (or being interrupted mid-way) — a stale-DB state, not something to design around or write a repair migration for. Resolves itself the next time the owner re-runs the migrations; not part of this build.

**Item (b) resolved**: the form-generation side already solved both sub-gaps, for a different entity, before Policy needed them —
   - Unbounded-depth recursion into nested array items: `collectOntologyTypesFromItemProps` (`formGenerator.ts:584`) already recurses fully — its own comment names the exact precedent (CR-088, Checklist's `contributionChecklists[].items[].configurableKey`, the same 2-levels-deep shape `conditions[].relatedObligations[].category` has).
   - Nested plain objects (not just arrays): `buildItemFields` (`formGenerator.ts:305`) recurses into `type: "object"` + `properties` items too (no `items`, Checklist's own precedent) — needed for `conditions[].requiredEvidence.category` (an object, not an array, one level under `conditions[]`).
   - Dynamic/by-value source: `ontologyComposableFieldsIn`'s `x-referential-source-by-value` branch (`formGenerator.ts:612-629`) already resolves a field's concept type per-submission from a sibling driver field's value, and checks the resolved `.ontology` flag before treating it as Ontology-governed — exactly `applicabilityDeliverables[].name`'s shape (driven by `scope`).
   `validateOntologyFieldsAgainstSchema` is the one piece still shallow/static-only, because it was only ever exercised against Pack/Template/Profile's flatter fields. Porting these three already-proven patterns into it is a mechanical extension, not new design.

### Proposed build scope (mirrors Template/Profile's own passes)

- Migration 269: add `policy_definitions.schema_definition_id` (backfilled to the current latest Policy schema — whichever version is live at migration time, same backfill pattern as 266/267/268). No table CHECK to drop (finding 1: nothing diverges).
- `core/ontology.ts`'s `validateOntologyFieldsAgainstSchema` — extended, not replaced, so Pack/Template/Profile's existing call shape and behaviour are unchanged:
  - Recurse into `type: "object"` item fields (not just `type: "array"`) the same way `buildItemFields` already does, so a nested object's own `x-ontology` fields (`conditions[].requiredEvidence.category`) get checked.
  - Recurse into nested arrays-of-arrays to unbounded depth, mirroring `collectOntologyTypesFromItemProps`'s own recursion, so `conditions[].relatedObligations[].category/origin/priority/severity` (2 levels of array nesting) get checked.
  - Add an `x-referential-source-by-value` branch to `resolveConceptType`'s write-time counterpart, mirroring `ontologyComposableFieldsIn`'s own byValue resolution (read the driver field's current value from the same row/content the target field sits in, resolve `.ontology`, skip enforcement if `false` or unresolved) — closes `conditions[].applicabilityDeliverables[].name` (Transition → real `deliverable-name` check; Eligibility → left alone, not Ontology, by design, same as today).
- New `core/policyDefinitionWriteValidator.ts` (mirrors `core/templateWriteValidator.ts`): `validatePolicyDefinitionWriteAgainstSchema` — fetches the pinned (`schemaDefinitionId`) or latest Policy schema, merges `{code, name, description, category, constraintType, applicabilityEnvironments, scope, conditions}` (real columns, all also schema properties) with `draftContent`, runs `validateAgainstSchema` (shape) + the extended `validateOntologyFieldsAgainstSchema` + a code+version+tenant uniqueness check (raw query, avoiding a `policyDefinitionsDB` <-> helper import cycle).
- `policyDefinitionsDB.ts`: both real write paths validated — `createDraft` and `updateDraftContent` (the only two; finding 6 — no `setDraftContent`/`upsert` exist for this entity) call the validator before the INSERT/UPDATE and return `{ error }` without writing on failure; `createDraft` stamps `schema_definition_id` to the current latest Policy schema; `updateDraftContent` pins to the row's own existing `schema_definition_id`. Both existing call sites (`sdkAuthoring.ts`'s `createAuthoringDraft`/`saveAuthoringDraft` Policy branches) already destructure `{ data, error }` and return `{ ok: false, errors }` on error, so no caller shape changes needed.
- `seuTypes.ts`: `PolicyDefinitionRow.schema_definition_id: string | null` added.
- `core/policyDefinitions.ts`: `validatePolicyDefinitionSeed`'s hand-coded checks left in place, not deleted — same conservative choice Pack/Template/Profile's own passes made (harmless duplication once the schema-driven path also runs). `applicabilityDeliverables[].name`'s Eligibility-branch check (a real Authority Vocabulary noun, never Ontology) and its `transitions[]` membership check stay regardless — genuine DB-lookup/cross-field logic, not schema-expressible even with the walker extended.

Not touched, by design: `updateStatus` (lifecycle-only, no content). `validateGoverningCondition`'s per-type shape rules, `exceptionRules[].exceptionApprovers` badge-vocabulary membership, and `parentPolicyDefinitionId` resolution all stay hand-coded — genuine DB-lookup/cross-field checks, same carve-out class as Pack's quality-gate checks.

## Built (Policy, first pass)

- migration 269: added `policy_definitions.schema_definition_id`, backfilled to whatever Policy schema is live (version 1, on this DB — see the resolved item (a) above). No CHECK dropped (finding 1: nothing diverges).
- `core/ontology.ts`'s `validateOntologyFieldsAgainstSchema` — extended in place (Pack/Template/Profile's existing call shape/behaviour unchanged, confirmed by walking through their own field shapes against the new code):
  - New recursive `walk()` replaces the old two-loop (top-level + one-level-into-array-items) version. Recurses into `type: "array"` fields with `items.properties` (Policy's `conditions[].relatedObligations[]`, 2 levels deep, and beyond if a future entity needed 3) exactly the way `formGenerator.ts`'s `collectOntologyTypesFromItemProps` already does, and into `type: "object"` fields with `properties` (no `items` — Policy's `conditions[].requiredEvidence`, a single sub-object, not a repeatable list) the way `buildItemFields`'s own "type: object" branch already does.
  - New `resolveByValueConceptType()` — the write-time counterpart of `ontologyComposableFieldsIn`'s own `x-referential-source-by-value` branch: resolves a field's concept type from a TOP-LEVEL driver field's current value (Policy's `conditions[].applicabilityDeliverables[].name`, driven by `scope`), skips enforcement when the resolved variant isn't Ontology-governed (scope=Eligibility — a noun, left to `validateConditions`'s own hand-coded branch) or is itself composable.
  - Bug fix, surfaced while extending: a top-level `type: "array"` field marked `x-ontology: true` with plain-string items (a real multi-select, not a referential-list of objects — Policy's own `applicabilityEnvironments`, Service Definition's `consumers`) was never actually being enforced — the old code called `checkValue` with the whole array as `value`, and `checkValue`'s own `typeof value !== "string"` guard silently no-op'd on every array. New `checkMulti()` iterates each selected value. Confirmed dormant for Service (no write validator wired for it yet, so nothing behavioural changes there today) and newly real for Policy's `applicabilityEnvironments`.
- New `core/policyDefinitionWriteValidator.ts` (mirrors `core/templateWriteValidator.ts`): `validatePolicyDefinitionWriteAgainstSchema` — pinned/latest Policy schema, merges `{code, name, description, category, constraintType, applicabilityEnvironments, scope, conditions}` (real columns, all also schema properties) with `draftContent`, runs `validateAgainstSchema` + the extended `validateOntologyFieldsAgainstSchema` + code+version+tenant uniqueness.
- `policyDefinitionsDB.ts`: both real write paths validated — `createDraft` and `updateDraftContent` (the only two; no `setDraftContent`/`upsert` exist for this entity) call the validator before the INSERT/UPDATE and return `{ error }` without writing on failure; `createDraft` stamps `schema_definition_id` to latest; `updateDraftContent` pins to the row's own existing `schema_definition_id`. Both existing call sites (`sdkAuthoring.ts`'s Policy branches, `copyPolicyDefinitionAsNewDraft`) already destructure `{ data, error }`, so no caller changes were needed.
- `seuTypes.ts`: `PolicyDefinitionRow.schema_definition_id: string | null` added.
- `core/policyDefinitions.ts`: `validatePolicyDefinitionSeed`'s hand-coded checks left in place, not deleted — same conservative choice as Pack/Template/Profile.

Test fixtures checked against the new enforcement (CLAUDE.md: schema-changing CRs list fixture impact in the same pass, not after the suite breaks) — both direct `policyDefinitionsDB.createDraft` callers that bypass `sdkAuthoring.ts`'s own already-blocking `validatePolicyDefinitionSeed` gate:
  - `tests/policy-definition-event-lifecycle-table.test.ts`'s `freshPolicyDefinitionDraft` — `category: "Engineering"`, a real registered `category:policy` concept (migration 107). No `conditions`/`applicabilityEnvironments` passed (default to `[]`). Passes.
  - `tests/testFixtures.ts`'s `ensurePolicyDefinitionWithObligation` (used by `cr104-mandatory-seu-eligibility-policies.test.ts`, `cr107-execution-engine-deliverable-kickoff.test.ts`, `cr108-obligation-model-seu-execution.test.ts`, all passing `category: "Compliance"`) — exercises exactly the newly-reachable deep fields: `conditions[].severity` ("High"), `relatedObligations[].category/origin/priority/severity` ("Compliance"/"Policies"/"Medium"/"High"). Verified every one is a real, registered concept (`category:policy-condition-severity`, `category:obligation`, `category:obligation-origin`, `category:obligation-priority`, `category:obligation-severity`) via direct query. Passes. `requiredEvidence`'s blank placeholder object (`{title:"",category:"",...}`) is harmless either way — the live (pre-224) schema doesn't even declare a `category` property on it yet, and the value is empty regardless.
  No fixture needed changes.

### Third layer: `pnpm test` — 21 failures, all fixture gaps of the exact same class

`pnpm test` (run by owner, output.txt) surfaced 21 failures. All 21 traced back to test-side callers of the now-validated write paths (`profilesDB.create`/`createDraft`/`setDraftContent`, `templatesDB.createDraft`/`setDraftContent`) omitting `purpose`/`baseTemplateCode` — the exact same class of gap as the two layers above, just in test fixtures and one more core call site instead of seed scripts. No new validator bug found; every fix below is a caller now supplying content the schema has required all along.

- **Core code gap** (not test-only): `createProfile` (core/profiles.ts, the `findOrCreateDefaultProfile`/`POST /profiles` path) called `profilesDB.create({ baseTemplateId, environment })` with no `baseTemplateCode` — `profilesDB.create`'s own signature never had the field. Fixed: `create` now takes `baseTemplateCode: string` (required) and merges it into the write-validator's `draftContent`; `createProfile` passes `template.code`. Covers `acceptance.e2e.test.ts` (API path) and `objective-lifecycle.test.ts` (direct call) — the only two failures caused by app code, not test fixtures.
- **Test fixture gaps**, one `draftContent`/seed addition each, no behavioural change to what's being tested:
  - `tests/testFixtures.ts` — `ensureWebAppTemplateFixture`'s `seed()` builds its parent Template via the unvalidated `templatesDB.upsert` (by design, never writes `draft_content`), so `purpose` never lands on it; `inheritedTemplateContent` (CR-026) reads `purpose` straight off `draft_content`, so every Draft inherited from this fixture failed. Fixed: one guarded `templatesDB.setDraftContent(template.id, { purpose })` call right after the upsert, added `purpose?` to the local `TemplateSeed` interface and to `web-application.template.json`. Also fixed the unrelated `ensureWebAppTemplateFixture` retry race: `profileWriteValidator.ts`'s new pre-INSERT uniqueness check can now return a clean `"already exists at version"` message instead of the raw `profiles_code_version_tenant_key` constraint text the retry-detection `.includes()` was matching on — widened the match to catch both.
  - `tests/template-event-lifecycle-table.test.ts` — `freshTemplateSeed`/`freshTemplateDraft` helpers had no `purpose`/`draftContent.purpose`. Added both.
  - `tests/profile-event-lifecycle-table.test.ts` — `freshProfileDraft` had no `draftContent.baseTemplateCode`. Added.
  - `tests/sdk-authoring.test.ts` — the two failing CR-026 tests both flow through `ensureWebAppTemplateFixture`/`inheritedTemplateContent`; fixed by the `testFixtures.ts` fix above, no edit needed in this file itself.
  - `tests/cr088-filter-shaped-overrides.test.ts` — two separate gaps: (1) `buildFixtureTemplate`'s direct `templatesDB.setDraftContent({ exposedParameters })` call never checked its own `error` return and had no `purpose`, so the write silently no-opped and every candidate-derivation test downstream saw an empty Template (3 of the 4 failures here, including the "valueBearing no longer required" one — a real CR-088 mechanism, not actually broken, just never seeing data); fixed by adding `purpose` and asserting the write succeeds. (2) `publishTemplate`'s own seed in the "materialises a non-sparse exposedParameters" test had no `purpose` (the 4th failure); added.
  - `tests/cr101-domain-configuration-competency-union.test.ts` — `createProfile` helper called `profilesDB.setDraftContent` (after an unvalidated `profilesDB.upsert`) without checking its `error` and without `baseTemplateCode`, so every Profile's draft content silently stayed empty and every union/conflict assertion saw nothing (all 4 failures here). Fixed: `createTemplate` now returns `{ id, code }` (was bare `id`) so `createProfile` can merge `baseTemplateCode: template.code` into the draftContent and assert the write's `error` is null; all call sites updated for the new return shape.
- **`tests/compliance-model.test.ts`** (4 failures, all "Profile ... already exists at version 1.0.0") — same root cause as the `ensureWebAppTemplateFixture` retry-race fix above (shared fixture), not a separate bug; fixed by that one change.

### Fourth layer: `pnpm test` re-run — 4 failures, all `cr088-filter-shaped-overrides.test.ts`

Down from 21 to 4, all in one file, same root cause: `buildFixtureTemplate` minted a fresh `test-cr088-template-${randomUUID()}` code per call. Template's `code` field has been `x-ontology: true` against `template-categories` (a small, fixed, real business vocabulary) since migration 054 — enforced at `publishTemplate` time via `validateTemplateSeed`'s own `assertCanonicalCategory` call, but this fixture bypasses `publishTemplate` entirely (raw `templatesDB.upsert`/`setMandatoryPacks`/`setDraftContent`), so it never hit that check — until Template's write-time validator (`validateOntologyFieldsAgainstSchema`, wired into `setDraftContent` this session) started enforcing it on this path too. Not a new rule; the first path to actually enforce an old one, same pattern as `purpose`/`baseTemplateCode` above.

CR-046/CR-079 already established (and `cleanSlate.ts`'s own `TEMPLATE_CATEGORY_TEST_CONCEPTS` array documents) the fix for exactly this situation, owner: *"the test script should use a code present in the ontology"* — a test needing its own dedicated Template identity gets one **stable, permanent** `template-categories` concept (seeded once, idempotently, by `db:clean-slate`), with per-call uniqueness moved to `templateVersion` (`uniqueTestPackVersion()`) instead of the code. `test-enterprise-web-application` and `test-cr088-publish-template` are exactly this pattern already, both live in that same array.

Fixed the same way: added `["test-cr088-overridable-template", "Test: CR-088 Overridable Parameters Fixture"]` to `TEMPLATE_CATEGORY_TEST_CONCEPTS` (`cleanSlate.ts`), and changed `buildFixtureTemplate` to upsert that one stable code with `templateVersion: uniqueTestPackVersion()` per call instead of a random code suffix. `deriveOverridableParameterCandidates`/`findActiveByCode` resolve by `ORDER BY created_at DESC LIMIT 1`, so each test still gets its own just-created row correctly.

**Needs a `db:clean-slate` run** before this concrete fix takes effect (the new concept row doesn't exist yet in whatever DB the next `pnpm test` run points at) — owner runs both, not run here.

## Deepdive on Service Definition

Same write-time-enforcement gap class as Pack/Template/Profile/Policy, on `service_definitions` (CR-086, Ch.11, migration 153, `serviceDefinitionsDB.ts`/`core/serviceDefinitions.ts`, entity_kind='Service').

1. **No table CHECK divergence.** Only `status` has a CHECK (structural, correct — same disposition as every other entity). No column carries a stray CHECK that duplicates what the schema's own `x-ontology`/enum properties already govern.
2. **`schema_definition_id` column: absent entirely**, not just unbackfilled — `service_definitions` was never given one, unlike Pack/Template/Profile/Policy (migrations 266–269). A write-time validator needs it to pin an update to the schema version a row was actually authored against.
3. **Ontology field depth/shape audit** (schema = migrations 153/156/159, entity_kind='Service' version 1):
   - `capabilityCode` — single string, `x-ontology:true`, `x-referential-source:"capability-name"` (migration 156). Top-level scalar — already reachable at the walker's original (pre-Policy-session) depth.
   - `inputs`, `outputs`, `consumers` — all three top-level `type:"array"` of plain strings, `x-ontology:true`, `x-referential-source:"deliverable-name"` (inputs/outputs, migration 159) or `"capability-name"` (consumers, migration 156). This is exactly the shape the Policy session's `checkMulti()` bug fix was written for — Service was the second of only two fields platform-wide with this shape, confirmed dormant at the time because no write validator called `validateOntologyFieldsAgainstSchema` for Service yet. Wiring one up now makes that fix live for the first time.
   - `serviceLevel` — array of objects (`code`, `label`, `target_level`, `target`, `units`; migration 208), no `x-ontology` on any sub-field — `target_level` is a plain inline `enum`, not Ontology-governed. Correctly not Ontology; no walker recursion needed here.
   - `code` — plain string, no `x-ontology`. Not a gap: matches Policy's own `code` (167) exactly — an entity's own registered identity field is hand-validated in core (`assertCanonicalCategory("service-name", ...)`, `core/serviceDefinitions.ts`) rather than schema-flagged, because the check is "does this identity already exist as a concept" (registration), not "pick from an existing vocabulary" (selection) — the same distinction Policy's deepdive settled for its own `code`.
   - `name`, `purpose`, `governance`, `success` — plain free text, correctly not Ontology.
4. **DB-layer write-path surface**: `serviceDefinitionsDB.createDraft`/`updateDraftContent` — the only two content-writing methods (no `setDraftContent`/`upsert`, same as Policy) — do zero validation today, not even a schema-shape check. Caller-side is already gated the same way Policy's is: `sdkAuthoring.ts`'s `createAuthoringDraft`/`saveAuthoringDraft`/advance-triggered-save Service branches all call `validateServiceDefinitionSeed` (blocking) before writing. `copyServiceDefinitionAsNewDraft` (`core/serviceDefinitions.ts`) bypasses it entirely, same bypass class as `copyPolicyDefinitionAsNewDraft` had.
5. **Test fixture check** (CLAUDE.md: list fixture impact before building, not after): `tests/service-definition-event-lifecycle-table.test.ts` is the only direct `serviceDefinitionsDB.createDraft` caller in the suite (its own comment: "raw DB-layer insert... same 'bare Draft, one hop at a time' fixture pattern" — deliberately bypasses `validateServiceDefinitionSeed`). It passes `capabilityCode: "test-capability"` on all three calls. Verified live: `"test-capability"` is **not** a registered `capability-name` concept — nothing in `cleanSlate.ts` seeds it (grepped for it directly, no hits outside this test file). Today this is silently unvalidated (the whole reason the fixture bypasses the seed function); once the DB layer itself validates, this fixture breaks. `inputs`/`outputs` values it passes (`requirements-specification`, `domain-model`, `source-code`) ARE real registered `deliverable-name` concepts — verified live, no issue there. **Needs fixing in the same build pass**: give `"test-capability"` a real, stable `capability-name` Ontology row in `cleanSlate.ts`, same treatment `TEMPLATE_CATEGORY_TEST_CONCEPTS` gives Template's own test fixture codes — not a random/never-seeded string.

### Proposed build scope (mirrors Policy's own pass) — not yet built

- Migration 270: `ALTER TABLE service_definitions ADD COLUMN IF NOT EXISTS schema_definition_id UUID REFERENCES schema_definitions(id)`, backfilled to `entity_kind='Service' ORDER BY version DESC LIMIT 1`. No CHECK dropped (finding 1: nothing diverges).
- New `core/serviceDefinitionWriteValidator.ts` (mirrors `policyDefinitionWriteValidator.ts`): `validateServiceDefinitionWriteAgainstSchema` — pinned/latest Service schema, merges `{code, name, capabilityCode, purpose, inputs, outputs, serviceLevel, governance, success, consumers}` (real columns, all also schema properties) with `draftContent`, runs `validateAgainstSchema` + `validateOntologyFieldsAgainstSchema` (this is what activates the `checkMulti()` fix for `inputs`/`outputs`/`consumers` for the first time) + code+version+tenant uniqueness.
- `serviceDefinitionsDB.ts`: wire the validator into both `createDraft` and `updateDraftContent`, return `{ error }` without writing on failure; `createDraft` stamps `schema_definition_id` to latest, `updateDraftContent` pins to the row's own existing `schema_definition_id` (select it first, same pattern as `policyDefinitionsDB.updateDraftContent`).
- `seuTypes.ts`: add `schema_definition_id: string | null` to `ServiceDefinitionRow`.
- `core/serviceDefinitions.ts`: `validateServiceDefinitionSeed` left in place, unchanged — same conservative choice as every other entity's pass.
- `cleanSlate.ts`: seed `"test-capability"` as a real, stable `capability-name` concept (own the fixture fix in this pass, not after the suite breaks).
- Not touched, by design: `updateStatus`, `syncOntologyOnActivate`/`demoteOntologyIfNoOtherActive`, `transitionServiceDefinition`'s transition-gate logic, `listInheritableServiceDefinitions`/`inheritedServiceDefinitionContent`.

## Built (Service Definition, first pass)

- Migration 270: added `service_definitions.schema_definition_id`, backfilled to whatever Service schema is live. No CHECK dropped (finding 1: nothing diverges).
- New `core/serviceDefinitionWriteValidator.ts` (mirrors `policyDefinitionWriteValidator.ts`): `validateServiceDefinitionWriteAgainstSchema` — pinned/latest Service schema, merges `{code, name, capabilityCode, purpose, inputs, outputs, serviceLevel, governance, success, consumers}` (real columns, all also schema properties) with `draftContent`, runs `validateAgainstSchema` + `validateOntologyFieldsAgainstSchema` (activates the Policy session's `checkMulti()` bug fix for `inputs`/`outputs`/`consumers` for the first time — all three are top-level arrays-of-strings, previously silently unenforced everywhere) + code+version+tenant uniqueness.
- `serviceDefinitionsDB.ts`: both real write paths validated — `createDraft` and `updateDraftContent` (the only two; no `setDraftContent`/`upsert`) call the validator before the INSERT/UPDATE and return `{ error }` without writing on failure; `createDraft` stamps `schema_definition_id` to latest; `updateDraftContent` selects the row's own existing `schema_definition_id`/`tenant_id` first and pins to it. All three existing call sites (`sdkAuthoring.ts`'s two Service branches, `copyServiceDefinitionAsNewDraft`) already destructure `{ data, error }`, so no caller changes were needed.
- `seuTypes.ts`: `ServiceDefinitionRow.schema_definition_id: string | null` added.
- `core/serviceDefinitions.ts`: `validateServiceDefinitionSeed` left in place, unchanged — same conservative choice as every other entity's pass.
- `tsc --noEmit` run: no new errors from any touched file (3 pre-existing, unrelated errors in `routeAuthorityRegistry.ts`).

Test fixture fix (CLAUDE.md: schema-changing CRs fix fixture impact in the same pass) — `tests/service-definition-event-lifecycle-table.test.ts`'s three direct `serviceDefinitionsDB.createDraft` calls all passed `capabilityCode: "test-capability"`, which is not a registered `capability-name` concept anywhere (confirmed live and by grep — nothing in `cleanSlate.ts` seeds it). Fixed by switching to `"requirements-analysis"`, a real registered `capability-name` code — following `seedTestFixturePacks.ts`'s own established precedent that `capability-name` gets **no** `test-`-prefixed twin (migration 151 removed the old dead-weight ones): "every test-fixture Pack's own contributions.capabilities[].code reuses the same shared canonical terms the real Packs do." No new Ontology concept added to `cleanSlate.ts`. `inputs`/`outputs` values (`requirements-specification`/`domain-model`/`source-code`) were already real, verified live — untouched. Comment above `freshServiceDefinitionDraft` updated to match.

## Deepdive + Build on Deliverable Definition

Same gap class as the previous four, on `deliverable_definitions` (CR-049 Phase 1, migration 081, `deliverableDefinitionsDB.ts`/`core/deliverableDefinitions.ts`, entity_kind='Deliverable').

Findings:
1. **No table CHECK divergence** — only `status`, structural, same as every other entity.
2. **`schema_definition_id` column: absent entirely**, same gap Service Definition had.
3. **Schema is genuinely tiny and has NO `x-ontology` fields at all** — `code` (plain string, `x-help` only), `description` (plain textarea), `definitionVersion` (plain semver-pattern string). `code` is hand-validated in core only in the sense that `syncOntologyOnActivate` REGISTERS it as a new `deliverable-name` concept on Active (not a pick-from-existing-vocabulary check) — same registration-not-selection distinction Policy's/Service's own `code` already established, correctly not schema-flagged. Consequence: `validateOntologyFieldsAgainstSchema` is a structural no-op for this entity today (nothing to walk), but wiring it in is still correct for consistency and because it's a zero-cost call, and it's what a future Ontology-governed field on this schema would need already in place.
4. **DB-layer write-path surface**: `createDraft`/`updateDraftContent` (only two write paths, no `setDraftContent`/`upsert`) do zero validation. Caller-side already gated: `sdkAuthoring.ts`'s `createAuthoringDraft`/`saveAuthoringDraft`/advance-triggered-save all call `validateDeliverableDefinitionSeed` (blocking) first. `copyDeliverableDefinitionAsNewDraft` and `reactivateAsNewVersion` (both in `core/deliverableDefinitions.ts`) bypass it entirely, same bypass class as the other four entities' own copy/reactivate paths.
5. **Test fixture check**: no test file calls `deliverableDefinitionsDB.createDraft`/`updateDraftContent` directly (grepped `tests/` and `src/` — only `sdkAuthoring.ts` and `core/deliverableDefinitions.ts` itself do). No fixture risk, no fixture change needed.

Built (mirrors Service Definition's own pass):
- Migration 271: `ALTER TABLE deliverable_definitions ADD COLUMN IF NOT EXISTS schema_definition_id UUID REFERENCES schema_definitions(id)`, backfilled to `entity_kind='Deliverable' ORDER BY version DESC LIMIT 1`. No CHECK dropped.
- New `core/deliverableDefinitionWriteValidator.ts` (mirrors `serviceDefinitionWriteValidator.ts`): `validateDeliverableDefinitionWriteAgainstSchema` — pinned/latest Deliverable schema, merges `{code, description}` (real columns, also schema properties) with `draftContent`, runs `validateAgainstSchema` + `validateOntologyFieldsAgainstSchema` (a no-op today per finding 3, future-proofing only) + code+version+tenant uniqueness.
- `deliverableDefinitionsDB.ts`: `createDraft`/`updateDraftContent` both validate before writing, return `{ error }` on failure; `createDraft` stamps `schema_definition_id` to latest, `updateDraftContent` selects the row's own existing `schema_definition_id`/`tenant_id` first and pins to it. All callers (`sdkAuthoring.ts`, `copyDeliverableDefinitionAsNewDraft`, `reactivateAsNewVersion`) already destructure `{ data, error }` — no caller changes needed.
- `seuTypes.ts`: `DeliverableDefinitionRow.schema_definition_id: string | null` added.
- `core/deliverableDefinitions.ts`: `validateDeliverableDefinitionSeed` left in place, unchanged.
- No fixture changes needed (finding 5).
- `tsc --noEmit`: clean on touched files.

## Schema gap check: Transition Definition

Different shape of gap than Pack/Template/Profile/Policy/Service/Deliverable — not "wire up write-time enforcement," but "two of the three checked sources are stale copies nothing live reads."

1. **Table `entity_type` CHECK — already resolved, not live.** Migration 036 dropped `transition_definitions_entity_type_check` outright (CR-006): "the noun vocabulary is now data (`authority_nouns`)... a new noun can now carry transitions without a migration." No CHECK exists today. Nothing to drop.

2. **`schema_definitions` (`entity_kind='TransitionDefinition'`, migration 016, v1) — vestigial, not authorable.** `schemaRegistry.ts`'s `SCHEMA_ENTITY_KINDS` (the list of kinds a new schema version can be authored for) is `["Pack", "Template", "Profile"]` only — CR-019's own comment: "TransitionDefinition is authored via the CR-007 /authority form (noun × verb), not a grammar... not a schema-registry authorable kind. Existing historical rows remain viewable; no new TransitionDefinition grammar versions." Requirement 2 (form generator uses the schema) does not apply — CR-019 deliberately opted this entity out of the schema-driven form entirely. The v1 row's own `entityType` enum (11 values: SEU, Deliverable, Objective, Obligation, Evidence, Knowledge, Decision, KnowledgeScope, AttentionItem, ExternalInteraction, Pack) is frozen at whatever the noun vocabulary was in migration 016 and never touched since.

3. **Three sources of truth for `entity_type`, only one live:**
   - Live, real: `authorityVocabularyDB`'s `nouns` (`authorityVocabulary.json`, seeded into `authority_nouns`) — 22 codes today, including Participant, Review, Finding, Template, Profile, TransitionDefinition, Ontology, QualityGate, Service, Policy, EBM on top of the original 11. This is what `addTransitionDefinition` (the real, only-live write path, wired to `sdkAuthoring.ts`'s web routes) actually checks against (`listActiveNouns()`), per migration 036's own stated intent.
   - Stale: `VALID_ENTITY_TYPES` in `core/transitionDefinitions.ts:18-30` — hand-listed, own comment admits it's "kept here as a plain list rather than introspecting the DB" — still the original 11, missing 11 of the 22 real nouns (Participant, Review, Finding, Template, Profile, TransitionDefinition, Ontology, QualityGate, Service, Policy, EBM).
   - Stale: migration 016's `schema_definitions` enum — same 11, same staleness, and per (2) is unreachable by any live form/validator anyway.
   - **Not stale, not part of the drift:** `TransitionEntityType` (`seuTypes.ts:1055-1096`) — 20 entries, matches the real `transitions[]` data in `authorityVocabulary.json` exactly (verified: the union's 20 codes = the seed's 20 distinct `entityType` values used across all 132 seeded transition rows). `TransitionDefinition`/`QualityGate` are real nouns (carry verbs/authority elsewhere) but never appear as a transition's own `entityType` — correctly absent from this type.

4. **Consequence: the stale list is dead code, not a live bug.** `VALID_ENTITY_TYPES` is only read by `validateTransitionDefinitionSeed`, which is only called by `publishTransitionDefinition` — and `publishTransitionDefinition` has zero callers anywhere in `src/routes` or `src/dblayer/seed` (grepped; only self-reference and one direct test call to `validateTransitionDefinitionSeed` itself, in `tests/transition-definition-authoring.test.ts`, exercising the referential checks, not the entity-type list). The real UI (`sdkAuthoring.ts` web routes, CR-007's Add/Edit/Retire actions) calls `addTransitionDefinition`/`updateTransitionDefinition`/`retireTransitionDefinition` directly, none of which touch `VALID_ENTITY_TYPES`, `schema_definitions`, or `validateAgainstSchema`. So today's actual behavior is correct (governed by the live noun table); the gap is a dead, silently-diverging duplicate sitting next to it, not a live enforcement hole.

Not a build proposal — flagging only, per the ask. If this is to be closed: `VALID_ENTITY_TYPES` is either deleted (with `validateTransitionDefinitionSeed`/`publishTransitionDefinition`, if confirmed fully dead) or rewritten to read `listActiveNouns()` like `addTransitionDefinition` does; migration 016's `schema_definitions` row is either left alone as a historical artifact (per CR-019's own framing) or a follow-up migration marks it retired/superseded explicitly. Both are one-line-scope decisions, not raised here as a recommendation.

## Built: Transition Definition schema/dead-code cleanup (owner's decision)

- Migration 272: `DELETE FROM schema_definitions WHERE entity_kind = 'TransitionDefinition'` — removes the vestigial migration-016 row entirely (owner chose delete over leaving it as a historical artifact). Not yet applied to any DB — owner runs it.
- `core/transitionDefinitions.ts`: deleted `VALID_ENTITY_TYPES`, `TransitionDefinitionSeedInput`, `validateTransitionDefinitionSeed`, `PublishTransitionDefinitionResult`, `publishTransitionDefinition` — `publishTransitionDefinition` had zero real callers (only `addTransitionDefinition`/`updateTransitionDefinition`/`retireTransitionDefinition`, wired to `sdkAuthoring.ts`, are live). Unused imports `authorityRulesDB`/`policiesDB`/`qualityGatesDB` removed with them. File header comment updated to drop the now-inaccurate SDK-pipeline framing.
- `tests/transition-definition-authoring.test.ts`: deleted the one test that called `validateTransitionDefinitionSeed` directly ("a Quality Gate code scoped to a different transition is rejected") and its now-unused import — owner's explicit choice (option 1: delete the function and its test, accepting the loss of that specific referential-check coverage rather than porting it elsewhere). The other two tests in the file (engine-enforcement generalization; mapping Allow-trigger behavior) don't touch the deleted code and are unaffected.
- Not touched: `transitionDefinitionsDB.upsert` (DB layer) — still real, used directly by `tests/governance-depth.test.ts`, `tests/quality-gate-generalization.test.ts`, `tests/transition-definition-authoring.test.ts`, `tests/sustained-pattern-generalization.test.ts`; only the dead `core/transitionDefinitions.ts` wrapper (`publishTransitionDefinition`) around it was removed. `schemaRegistry.ts`'s `SCHEMA_ENTITY_KINDS` already excluded `TransitionDefinition` — no change needed there.


------

Reworked to finish CR-112's own deferred cleanup instead of adding more hardcoded per-field checks:

ontology.ts: new validateComposableFieldsAgainstSchema(schema, content, viewer) — the publish-time counterpart of the existing proposeComposableOntologyValues, reusing the same schema-driven ontologyComposableFieldsIn (formGenerator.ts, untouched). One generic function covers every x-ontology-composable field platform-wide; no hardcoded concept-type strings, no form-generator changes.
packs.ts / templates.ts: validatePackSeed/validateTemplateSeed no longer hand-code category:pack/${category}-name/template-categories — they fetch the entity's own schema_definitions row and call validateOntologyFieldsAgainstSchema (non-composable, hard-check) + optionally validateComposableFieldsAgainstSchema (composable, skippable via a skipComposableChecks option). Deleted the per-entity hand-rolled validatePackComposableFields.
policyDefinitions.ts: same treatment for applicabilityEnvironments, decoupled from the pre-existing draft flag (which still correctly governs the non-composable applicabilityDeliverables[].name). Deleted validatePolicyDefinitionComposableFields.
profiles.ts: deleted validateProfileComposableFields; environment now goes through the same generic function.
sdkAuthoring.ts: publishAuthoringDraft's Validated→Published hop for Pack/Profile/Policy/Template now calls validateComposableFieldsAgainstSchema directly with each entity's own schema and content — no per-entity wrapper. Also added the missing proposeComposableOntologyValues call to Template's createAuthoringDraft/saveAuthoringDraft (it never had one).
Migration 274: marks Pack.category and Template.code x-ontology-composable: true — the only change needed for these two fields; no other code touched for them specifically, confirming the pattern is now "just a schema change" for CR-113's remaining 12 fields (Dependency types, Deliverable/Knowledge/Evidence/Decision/Obligation/Policy/Review/Quality Gate/Event/Attention/Interaction categories) — each will need its own schema x-widget: referential-select/-multi-select + x-ontology + x-ontology-composable markers, but no new validator code, provided their existing hand-coded checks get the same treatment when we get to them.