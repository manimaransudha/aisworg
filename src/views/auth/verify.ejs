<%- include('../partials/head') %>

<style>
  body { background: #f1f5f9; }
  .verify-card {
    max-width: 420px;
    margin: 80px auto;
    background: #fff;
    border-radius: 16px;
    box-shadow: 0 4px 24px rgba(0,0,0,0.08);
    padding: 2.5rem 2rem;
  }
  .brand { text-align: center; margin-bottom: 1.75rem; }
  .brand h1 { font-size: 1.5rem; font-weight: 700; color: #1e293b; margin: 0; }
  .form-control { border-radius: 8px; }
  .btn-primary  { border-radius: 8px; width: 100%; }
</style>

<main>
  <div class="verify-card">
    <div class="brand">
      <h1>📈 CapWise</h1>
    </div>
    <h5 class="mb-1">Set your password</h5>
    <p class="text-muted small mb-4">Creating account for <strong><%= email %></strong></p>

    <% if (locals.error) { %>
      <div class="alert alert-danger py-2 small"><%= error %></div>
    <% } %>

    <!-- <form action="/finanaly/auth/verify" method="POST"> -->
    <form action="/aisworg/auth/verify" method="POST">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <input type="hidden" name="token" value="<%= token %>">
      <div class="mb-3">
        <label for="password" class="form-label small fw-semibold">Password</label>
        <input type="password" class="form-control" id="password" name="password"
               minlength="8" required autofocus autocomplete="new-password"
               placeholder="At least 8 characters">
      </div>
      <div class="mb-4">
        <label for="password_confirm" class="form-label small fw-semibold">Confirm password</label>
        <input type="password" class="form-control" id="password_confirm" name="password_confirm"
               minlength="8" required autocomplete="new-password">
      </div>
      <button type="submit" class="btn btn-primary">Activate account</button>
    </form>
  </div>
</main>

<%- include('../partials/footer') %>
