<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head') %>
  <%# CR-083 — .principle-card/.layer-card/.layer-header/.component-row/
       .component-name/.component-note/.status-badge (base)/.stat-tile/
       .stat-value/.stat-label all now come from the canonical stylesheet.
       .intro-lead and the .status-live/-partial/-deferred colour variants
       are this page's own (only used here) and were dropped along with
       everything else above without being restored anywhere — genuinely
       unstyled until now. Kept live, not centralised, since nothing else
       uses them. %>
  <style>
    .intro-lead { font-size: 1.05rem; color: #334155; line-height: 1.6; max-width: 760px; }
    .status-live { background: #dcfce7; color: #166534; }
    .status-partial { background: #fef9c3; color: #854d0e; }
    .status-deferred { background: #f1f5f9; color: #64748b; }
  </style>
</head>
<body>
  <header><%- include('../partials/navbar') %></header>

  <main class="container py-5">
    <% if (locals.flash) { %>
      <div class="alert alert-<%= flash.type === 'error' ? 'danger' : 'success' %> alert-dismissible fade show border-0 shadow-sm mb-4" role="alert">
        <i class="bi bi-<%= flash.type === 'error' ? 'exclamation-triangle' : 'check-circle' %> me-2"></i><%= flash.message %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>

    <div class="mb-4">
      <h2 class="h3 mb-2 text-dark fw-bold">SEU Commissioning Platform</h2>
      <p class="intro-lead mb-3">
        An execution platform that governs engineering organisations, not a coding assistant or a workflow tool.
        It starts from an Objective and assembles the Capabilities, roles and Participants — human, AI, or
        external — required to deliver it. Authority can't be exercised outside a defined responsibility, and
        work can't be marked complete without evidence behind it. None of that is policy or convention; it's
        enforced by continuous execution of the governance model itself, on every Software Engineering Unit
        (SEU) the platform runs.
      </p>
    </div>

    <div class="row g-3 mb-4">
      <div class="col-md-3 col-6">
        <div class="principle-card">
          <i class="bi bi-people-fill mb-2 d-block"></i>
          <div class="principle-title">Participants don't self-certify</div>
          <div class="principle-note">Human, AI, or external — finishing work is a proposal, not a fact. The same governance decides whether it takes effect, regardless of who or what proposed it.</div>
        </div>
      </div>
      <div class="col-md-3 col-6">
        <div class="principle-card">
          <i class="bi bi-diagram-3-fill mb-2 d-block"></i>
          <div class="principle-title">Lifecycle is enforced, not decorative</div>
          <div class="principle-note">Every Deliverable moves through a real, checked state graph. An illegal transition is rejected; a legal one with no justification behind it is too.</div>
        </div>
      </div>
      <div class="col-md-3 col-6">
        <div class="principle-card">
          <i class="bi bi-shield-check mb-2 d-block"></i>
          <div class="principle-title">Evidence is a precondition</div>
          <div class="principle-note">The Trust Pipeline: information into evidence, evidence supporting knowledge, knowledge informing a decision that justifies the state actually changing.</div>
        </div>
      </div>
      <div class="col-md-3 col-6">
        <div class="principle-card">
          <i class="bi bi-activity mb-2 d-block"></i>
          <div class="principle-title">Telemetry measures flow, not people</div>
          <div class="principle-note">Where work gets stuck — dependency wait, governance friction, rework — not who did what, when. A sustained pattern raises a real obligation automatically.</div>
        </div>
      </div>
    </div>

    <div class="mb-4">
      <p class="text-muted small mb-0">
        Laid out below against the platform's own architecture diagram
        (<code>design/foundations/03_Book 3 (Refined)/Introduction.md</code> — "The architecture becomes").
        <span class="status-badge status-live">Live</span> is built and working today,
        <span class="status-badge status-partial">Partial</span> is a minimal real instance, not the full model,
        <span class="status-badge status-deferred">Deferred</span> is out of MVP scope.
      </p>
    </div>

    <div class="row g-3 mb-4">
      <div class="col-md-4">
        <a href="/aisworg/seu/seus" class="text-decoration-none">
          <div class="stat-tile">
            <div class="stat-value"><%= counts.seuCount %></div>
            <div class="stat-label">SEUs Commissioned</div>
          </div>
        </a>
      </div>
      <div class="col-md-4">
        <a href="/aisworg/seu/packs" class="text-decoration-none">
          <div class="stat-tile">
            <div class="stat-value"><%= counts.packCount %></div>
            <div class="stat-label">Packs Loaded</div>
          </div>
        </a>
      </div>
      <div class="col-md-4">
        <div class="stat-tile">
          <div class="stat-value"><%= counts.eventCount %></div>
          <div class="stat-label">Events Published</div>
        </div>
      </div>
    </div>

    <div class="d-flex justify-content-end mb-3">
      <a href="/aisworg/seu/seus/new" class="btn btn-primary btn-sm"><i class="bi bi-plus-lg me-1"></i>Commission a new SEU</a>
    </div>

    <% layers.forEach(function(layer) { %>
      <div class="layer-card">
        <div class="layer-header"><%= layer.name %></div>
        <div>
          <% layer.components.forEach(function(c) { %>
            <div class="component-row">
              <div>
                <div class="component-name">
                  <% if (c.href) { %><a href="<%= c.href %>" class="text-decoration-none"><%= c.name %></a><% } else { %><%= c.name %><% } %>
                </div>
                <div class="component-note"><%= c.note %></div>
              </div>
              <span class="status-badge status-<%= c.status %>"><%= c.status %></span>
            </div>
          <% }) %>
        </div>
      </div>
    <% }) %>

    <% if (counts.recentEvents.length > 0) { %>
      <div class="layer-card">
        <div class="layer-header">Recent Runtime Kernel Events</div>
        <div class="table-responsive">
          <table class="table table-sm mb-0">
            <thead class="table-light"><tr><th>Type</th><th>Object</th><th>When</th></tr></thead>
            <tbody>
              <% counts.recentEvents.forEach(function(e) { %>
                <tr>
                  <td class="small"><code><%= e.event_type %></code></td>
                  <td class="small text-muted"><%= e.originating_object_type %></td>
                  <td class="small text-muted"><%= new Date(e.occurred_at).toLocaleString() %></td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>
    <% } %>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
